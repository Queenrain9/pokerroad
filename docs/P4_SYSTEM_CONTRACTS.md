# P4 system-contract closure — competition / save-resume / presentation\n\nDate: 2026-09-25\n\nThis milestone closes structural production contracts, not final game balance or visual art approval.\n\n## Competition / tournament / Q / AI\n\nImplemented competition_contract_v1.json and CompetitionContract with explicit event lifecycle, separated result domains, Q resolution, national-champion conditions, retry identity rules and the invariant that loss/tower skip/Q=NO or PENDING/wallet=0 cannot block the main route. PokerAI enforces a hard information boundary: it may use its own cards and public information, never opponent hole cards, future deck order, scripted player choices or future story results. Difficulty profile numbers remain tuning values.\n\n## Save / interruption recovery\n\nImplemented schema-v2 session contract, full BettingRound / HoldemHand / TournamentDirector snapshots, exact deck/board/hole/action/bet/stacks recovery, and world/dialogue/poker mode validation. GameState exposes v2 world-session snapshot/save/load APIs while the legacy v1 world-only save remains intact for compatibility. Mid-hand restore is tested deterministically; reward and story progress remain separate from loading.\n\n## Camera / space / controls / poker UI production grammar\n\nImplemented presentation_contract_v1.json and runtime validation. It fixes semantic mode transitions and all eight region capabilities before maps are built: moving cruise world vs fixed card HUD, real vertical forest layers, tide safe routes, free night bus, same-world palace day/night, market bonus activity not main gate, and Central City public/registered zones with an eight-seat final. Final art style, exact projection/zoom, proportions and final device layout stay explicitly provisional.\n\n## Acceptance boundary\n\nThis milestone is complete only when Python source-contract QA and Godot native headless tests pass after the changes. It does not claim playable maps 8/8, playable scenes 88/88, final AI strength/balance, final event field sizes/cutoffs/blinds/antes/economy, final camera/art/UI skin, or Windows/iPhone/Android device validation.\n\nThe next production stage after this contract passes is cross-region implementation preparation, then actual map/scene production.

## Acceptance result

Accepted on GitHub main after iterative native fixes:
- Python/source-contract QA: **87 passed**
- Exact five-card exhaustive classification: **2,598,960 / 2,598,960**
- Godot 4.4.1 Linux headless core: **31 checks passed**
- Godot P3 regression: **48 checks passed**
- Godot P4 system contracts: **37 checks passed**

Native failures found during this work (literal newline escapes, typed-array JSON restoration, numeric JSON restoration) were fixed before acceptance. Windows/iPhone/Android remain separate device gates.
